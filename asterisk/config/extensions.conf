[general]
static=yes
writeprotect=no

[voice-guard]
; Audio tapping via ChanSpy/AudioSocket would go here for ARI
; For demo web<->web, just dial directly
exten => 1001,1,NoOp(Call to 1001)
 same => n,Dial(PJSIP/1001,30)
 same => n,Hangup()

exten => 1002,1,NoOp(Call to 1002)
 same => n,Dial(PJSIP/1002,30)
 same => n,Hangup()

exten => 1003,1,NoOp(Call to 1003)
 same => n,Dial(PJSIP/1003,30)
 same => n,Hangup()

exten => 1004,1,NoOp(Call to 1004)
 same => n,Dial(PJSIP/1004,30)
 same => n,Hangup()

exten => 1005,1,NoOp(Call to 1005)
 same => n,Dial(PJSIP/1005,30)
 same => n,Hangup()

; Echo test for detection demo without second browser
exten => 9196,1,NoOp(Echo test)
 same => n,Answer()
 same => n,Echo()
 same => n,Hangup()

; Stasis app for ARI audio tapping (backend ARI client dials into this)
exten => 7000,1,NoOp(ARI Stasis entry)
 same => n,Stasis(voice-clone-guard,snoop)
 same => n,Hangup()
